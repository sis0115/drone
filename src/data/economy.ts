/**
 * SP 경제 — 05 문서 4장.
 *
 * ⚠️ 원칙(CLAUDE.md 규칙 5): **유료는 시간만 판다.** 성능 P2W·확률형 상자·FOMO 타이머 금지.
 * 여기 있는 것은 전부 플레이로 버는 값이다.
 */

/** 표적 가치표 (05 문서 4.1). 확인(BDA) 시 2배 — 배수는 CONFIRM_MULTIPLIER. */
export const SP_VALUE = {
  truck: 40,
} as const;

/**
 * 확인(BDA) 배수 — 05 문서 4.1 "군용 트럭 40 / 확인 시 80".
 *
 * **Ch.1 의 주제가 확인 킬 학습이다**(03 문서 1막): "고스트가 사진을 찍어야 실적이 된다.
 * 확인 없는 격파는 소문일 뿐이야." 그래서 첫 미션에서는 고스트가 **자동으로** 확인해 준다 —
 * 개념을 숫자로 보여 주는 게 목적이다. 플레이어가 확인을 책임지는 BDA 게임플레이
 * (정찰기 촬영·귀환)는 v0.3 이고, 그때 이 배수의 조건이 붙는다.
 */
export const CONFIRM_MULTIPLIER = 2.0;

/**
 * 첫 완수 보너스 — 프롤로그의 약속("격파를 증명하면, 장비가 갑니다")을 회수하는 값.
 * 미션당 1회, 최초 완수에만. 반복 파밍으로는 안 나온다.
 * 100 SP 는 05 문서 4.2 앵커("작전 요청 1회 평균 150 SP") 안쪽이다.
 */
export const FIRST_CLEAR_BONUS = 100;

/**
 * 차수 배율 (05 문서 4.3.2). 도전 보너스는 출격 전 옵션(v0.3)에서 붙는다.
 * 배율 상한 2.5 는 그때 함께 온다 — 지금은 차수 배율만.
 */
export const TIER_MULTIPLIER = [1.0, 1.4, 1.8] as const;

/**
 * 기체 손실 페널티: 기체 가격의 5% (05 문서 4.3). 회수 귀환 시 0.
 * T1 스패로우-7 은 기본 지급(가격 0)이라 현 콘텐츠에서 페널티도 0 —
 * 수식은 지금 세워 두고 값은 T2+ 기체와 함께 온다.
 */
export const LOSS_PENALTY_RATE = 0.05;

/** 기체 가격표 (05 문서 4.3 초안). 상점(v0.3)이 읽는다. */
export const FRAME_PRICE: Record<string, number> = {
  'frame.sparrow7': 0, // 기본 지급
};
