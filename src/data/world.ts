/**
 * 월드 생성 시드.
 *
 * 배치(바위·나무·건물·간이 도로)는 전부 난수로 깔린다. 시드가 없으면
 * **새로고침마다 지형지물이 달라져서** 미션(T8)이 "엄폐물 뒤에서 접근" 같은 설계를
 * 할 수 없고, 스크린샷 비교도 의미가 없어진다(점검 스윕에서 실제로 매번 다른 맵이 나왔다).
 *
 * 지형 높이(fbm)는 원래 결정적이었다 — 달라지던 것은 **배치**뿐이다.
 */
export const WORLD_SEED = 20260826;
