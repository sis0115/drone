/**
 * 작전 구역(AO) — T8b.
 *
 * GDD M2 실패 조건 "표적 이탈(맵 밖)"이 성립하려면 **맵 밖이라는 규칙**이 먼저 있어야 한다.
 * 안개(FOG.far 540m)는 시야를 가릴 뿐 경계가 아니다 — 시야와 규칙은 다른 일이고 둘 다 필요하다.
 *
 * 서사는 신호다: AO 는 임의의 벽이 아니라 **중계기 커버리지의 끝**이다.
 * 그래서 이탈하면 벽에 막히는 게 아니라 신호가 3초에 걸쳐 무너지고 링크가 끊긴다.
 */
export const AO = {
  /** 조종소(원점) 기준 반경 (m). 콘텐츠 배치(±470)를 덮고 지형(±800) 안쪽이다 */
  radius_m: 490,
  /** 경계 앞 경고 밴드 — 넘기 전에 알아야 돌아올 수 있다 */
  warn_band_m: 45,
  /** 이탈 후 링크가 완전히 끊기기까지의 유예 (초) */
  grace_s: 3,
} as const;
