# SIGNAL LOST : FPV — 작업 규칙

저화질 FPV 드론 전쟁 시뮬레이터. 모바일 웹 우선(Three.js + Vite + TypeScript), 이후 Steam.

## 먼저 읽을 것

1. `README.md` — 현재 상태와 다음 할 일
2. `docs/02_DEV_SPEC_web.md` — **구현 기준서.** 스택·구조·검증된 상수·성능 예산·태스크(T1~T10)
3. `DEVLOG.md` — 지난 세션의 결정과 밟은 함정. **같은 함정을 두 번 밟지 말 것**

문서 충돌 시 우선순위: `02` > 나머지 `docs/` > 코드 주석.
`01_GDD.md` 는 **게임 설계** 기준이지 구현 기준이 아니다(일부 Unity 시절 서술이 남아 있음).

## 절대 규칙

1. **검증된 물리·렌더 상수를 임의로 바꾸지 말 것** (`src/data/flight.ts`, `render.ts` / 02 문서 4장).
   바꿔야 하면 근거 수치와 함께 `DEVLOG.md` 에 기록한다.
2. **인스턴싱 필수.** 반복 오브젝트를 개별 `Mesh` 로 만들면 드로우콜이 1,940까지 폭증한다(실측).
   예산은 **드로우콜 < 120 / 삼각형 < 300k**.
3. **에셋(3D 모델·텍스처 파일) 도입 금지.** 재미 검증 전까지 프로시저럴 유지.
4. **UI 금지 목록** (04 문서 6장): 라운드 버튼, 그라데이션, 그림자, 코인 아이콘, "축하합니다!" 류.
   판정 기준 — *"이게 야전 단말기 화면에 있을 법한가?"* 컬러는 정의된 8색 밖으로 나가지 않는다.
5. **수익화**: 유료는 시간만 판다. 성능 P2W·확률형 상자·FOMO 타이머 금지.
6. **하드코딩 금지**: 문자열은 `src/i18n`(원본 `docs/strings_master.csv`), 튜닝 값은 `src/data/`.
7. **시스템 간 직접 참조 금지** — 교차 통신은 `src/core/EventBus.ts`.
   화면 전환은 `ctx.go()`, 다른 시스템 통지는 EventBus.
8. **계층 의존 방향** (02 문서 2.1) — 아래가 위를 부르지 않는다:
   `main → app → {ui, render, world, drone, input, mission, economy} → core → platform → {data, i18n}`
   - `world/` 는 **렌더러를 몰라야 한다** (그래야 `npm run scene` 이 브라우저 없이 돈다)
   - `main.ts` 는 배선만, 20줄 미만
   - `tests/architecture.spec.ts` 가 강제한다. 위반하면 테스트가 깨진다
9. `prototype/signal_lost_fpv.html` 은 **기준선이라 수정 금지.** 이식은 복사해서 한다.
10. **기기에 닿는 것은 `src/platform/` 을 통해서만.** 저장·진동·화면잠금·safeArea.
    직접 `localStorage`/`navigator.vibrate` 를 부르면 모바일 이식 때 전역 수색이 된다.

## 검증 (커밋 전 필수)

```bash
npm run verify    # typecheck → build → harness → perf → scene → playwright
npm run scene     # 코드베이스 드로우콜 실측 (예산 초과 시 종료 코드 1)
npm run compare   # 프로토타입 ↔ 코드베이스를 같은 조건으로 렌더, 스크린샷 2장
```

- **프로토타입 코드 자체가 기준선이다.** 상수를 추정으로 채우지 말 것 —
  T1 에서 조명·안개·카메라 값을 추정해 넣었다가 전부 틀렸다(DEVLOG 2026-08-26).
  `npm run compare` 가 그런 이탈을 잡는다.
  단, **아트 패스 1 이후 색·대기는 의도적으로 프로토타입과 다르다**(DEVLOG 2026-08-26).
  compare 는 구조 회귀(왜곡·레이아웃·HUD) 확인용이지 색 일치 확인용이 아니다.
- 스크린샷은 `tests/__screenshots__/` 에 남는다. **눈으로 확인할 것** — 테스트 통과 ≠ 화면이 맞음.
- 태스크 종료 시: 검증 → 스크린샷 확인 → 커밋(`T3: flight models`) → `DEVLOG.md` 갱신.

## 배포

- `develop` 에서 개발, 마일스톤마다 `main` 병합. `main` = 프로덕션.
- `/` = **코드베이스**, `/prototype.html` = 프로토타입 기준선(비교용).
- HUD 우하단 빌드 스탬프(`브랜치 커밋해시`)로 폰에서 반영 여부를 확인한다.

## 이 환경에서 안 되는 것

- **헤드리스 크로미움이 외부 CDN 에 못 나간다.** 배포된 URL 을 브라우저로 열어 검증할 수 없고,
  프로토타입 데모의 실제 씬 렌더도 확인할 수 없다. **실기 확인은 사람이 폰으로 해야 한다.**
- **여기 fps 는 실기와 무관하다.** SwiftShader 소프트 렌더라 프로토타입·코드베이스 모두 ~1fps 다.
  Playwright 타임아웃이 120초인 이유. **두 쪽 성능은 동등하다** — 회귀 없음(실측).
- **⚠️ 프로토타입의 fps 표시를 믿지 말 것.** 클램프된 dt 를 누적해 계산해서
  **20fps 아래로 내려가지 않는다**(진짜 1.1fps 인데 22fps 표시). 실기 측정은 **`/`(코드베이스)** 에서.
- **폰 실기 fps 미측정** — 프로젝트 최우선 미해결 항목. 45 미만이면 조정 순서는
  풀 인스턴스 수 → 그림자 해상도 → 덤불 수.
