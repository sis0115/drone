/**
 * 검증된 물리 상수 — 02 문서 4.1 / 4.2.
 * ⚠️ 임의 변경 금지. 바꿔야 하면 근거 수치와 함께 DEVLOG.md에 기록할 것.
 */

/** 프로 모드 (실물리, 기울기 기반). 실측: 호버 10초 드리프트 0.0000m, 최고속도 73.8km/h */
export const PRO = {
  MASS: 1.2,
  G: 9.81,
  MAX_THRUST: 35,
  MAX_TILT: (32 * Math.PI) / 180,
  TILT_RESP: 6.0,
  MAX_VS: 4.0,
  VS_KP: 3.2,
  YAW_RATE: 2.1,
  DRAG_H: 0.28,
  DRAG_V: 0.25,
} as const;

/** 아케이드 모드 (기본값, 속도 기반 + 지형 자동 추종). 실측: 전진 3초 79.2km/h, 고도 오차 0.08m */
export const ARCADE = {
  spd: 22,
  acc: 3.2,
  turn: 2.0,
  strafe: 11,
  climb: 14,
  aglMin: 4,
  aglMax: 140,
  /** 지형 고도 추종 게인 / 응답 */
  aglGain: 2.4,
  aglResp: 5.0,
  /** 장애물 충돌: 이 속도 미만이면 밀려나고, 이상이면 격추 */
  crashSpeed: 16,
} as const;

/**
 * 아케이드(풀어시스트)에 남는 바람 비율.
 *
 * ⚠️ 프로토타입에는 아케이드 바람이 아예 없었다(프로 모드에만 있었다).
 * 그런데 아케이드가 **기본 모드**이고 GDD 6.1 은 캠페인 2차수를 "바람 추가"로
 * 난이도를 올리므로, 기본 모드에 바람이 없으면 그 설계가 성립하지 않는다.
 *
 * 해법: 바람은 월드의 힘이고 **어시스트 단계가 상쇄율을 정한다**(GDD 7장 어시스트 3단계).
 * 풀어시스트는 비행 컨트롤러가 대부분 잡아 주고 30% 만 남긴다 —
 * 흐름은 느껴지되 조작이 무너지지 않는 선. 세미/ACRO 는 T4 입력 작업에서 단계를 나눈다.
 *
 * 이 값은 프로토타입에서 검증된 상수가 아니라 **새로 도입한 튜닝 값**이다 (DEVLOG 2026-08-26).
 */
export const ARCADE_WIND_COMPENSATION = 0.3;

/**
 * ACRO(레이트 모드) 기울기 변화율 rad/s.
 * 각도 모드(세미)는 스틱 위치가 곧 기울기지만, ACRO 는 스틱이 **각속도**를 준다 —
 * 놓아도 수평으로 돌아오지 않아 직접 반대로 눕혀야 한다. 고수용.
 *
 * ⚠️ 프로토타입에는 ACRO 가 없었다. GDD 7장 어시스트 3단계를 구현하며 새로 도입한 값이다.
 * MAX_TILT(32°) 를 약 0.4초에 찍는 속도로 잡았다.
 */
export const ACRO_TILT_RATE = 1.4;

/** 표적 판정 반경 (m) */
export const HIT_RADIUS = { arcade: 7.0, pro: 4.2 } as const;

/**
 * 하네스·테스트가 회귀를 잡는 기준선. 02 문서 4.1/4.2 실측치.
 *
 * ⚠️ 문서의 "최고속도 73.8km/h" 는 실은 **10초 스프린트 시점의 값**이다.
 * 같은 상수로 끝까지 가속하면 78.8km/h 에서 수렴한다(무풍, 해석해와 일치).
 * 둘 다 테스트로 고정해 둔다 — 문서 수치를 재현하면서 진짜 상한도 함께 지킨다.
 */
export const VERIFIED = {
  pro: { hoverDrift10s_m: 0.0, sprint10s_kmh: 73.8, topSpeed_kmh: 78.8 },
  arcade: { forward3s_kmh: 79.2, aglError_m: 0.08, turn1s_deg: 115 },
} as const;
