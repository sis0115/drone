# SIGNAL LOST : FPV — 프로젝트 핸드오프

> **이 파일을 가장 먼저 읽을 것.** 이후 작업은 `docs/` 기준.
> 핸드오프 대상: Claude Code (개발 착수) / 작성일 2026-08-25

---

## 1. 무슨 프로젝트인가

**저화질 FPV 드론 전쟁 시뮬레이터.** 모바일 웹 우선, 이후 Steam 확장.

- **핵심 감성**: 480p 흑백/열화상 아날로그 화면 너머의 전쟁. 조종사는 참호가 아니라 벙커 모니터 앞에 있다.
- **죽으면 게임이 죽는 3가지**:
  1. 저화질 카메라 연출 (성능 절약 + 독보적 감성 + 에셋 경쟁 회피)
  2. "실적 → 보급" 진행 시스템 (실제 킬 포인트 제도의 게임화)
  3. 실전 기반 미션 설계 (9종 시나리오 + 위협 카탈로그 16종)
- **세계관**: 실제 전쟁 모티프 + 국가명 추상화(베리스카 공화국 / 연방) — 스토어 심사 리스크 회피

## 2. 현재 상태

- 기획 문서 7종 완성 (`docs/`)
- **동작하는 프로토타입 v0.7 존재** (`prototype/signal_lost_fpv.html`) — 단일 HTML, 외부 에셋 0
- **헤드리스 검증 도구 동작** (`tools/`) — 브라우저 없이 Three.js 씬 검증
- **T1 완료** — Vite + TypeScript 스캐폴딩, 480×270 3버퍼 렌더 파이프 골격, `window.__debug` 훅
- **배포 연결 완료** — https://drone-azure-rho.vercel.app + CI
  - `/` = **코드베이스** (지금 볼 것은 이쪽)
  - `/prototype.html` = 프로토타입 v0.7 기준선 (비교용)
- **T2 완료** — 프로토타입 씬을 `src/world/`·`src/render/` 로 분해 이식.
  드로우콜 **62** (프로토타입 116 대비 -54), 장애물 29개 일치
- **T3 완료** — 비행 2종(Arcade/Pro) + 바람·배터리·LOS 차폐. 물리 테스트 14종이 실측값을 고정
- **구조화 완료** — `app/`(수명주기·화면) + `platform/`(웹↔모바일 분기점) 분리.
  `main.ts` 199줄 → 19줄. 계층 의존 방향을 `tests/architecture.spec.ts` 가 강제
- **T4 완료** — 가상 패드 2개(고정+플로팅) / 키보드 / 스크립트, 스틱 Mode 1·2, 어시스트 3단계
- **T5 완료** — HUD(점선 십자·코너 분산) + 표적 오버레이(락온/원거리) + 표적 트럭 3대
- **T6 완료** — 카메라 3모드(BW / COLOR / THRM). 열화상은 셰이더 리맵이 아니라
  **머티리얼 스왑** — 오브젝트마다 열값을 달아 두고 모드 전환 때 갈아 끼운다.
  하늘·물이 검고 트럭 엔진부만 백열로 뜬다
- **🏁 v0.1 (T1~T6) 완료 — 재미 검증 지점.** 조작 가능한 저화질 FPV 비행 + 열화상이
  브라우저에서 끝까지 돈다. **폰 실기 확인이 필요한 시점**(`/` 에서 측정, `/prototype.html` 아님)
- **T7 완료** — 위협 프레임워크. `Threat` 인터페이스 + **텔레그래프 계약** + 대표 2종
  (A1 산탄총 / B1 재밍 돔). "모든 위협은 예고된다"(GDD 4.5 규칙 1)를 주석이 아니라
  `ThreatRunner` 가 강제한다 — 예고 0.5초를 못 채운 격추 요청은 폐기되고 위반으로 기록된다
- **전체 점검 스윕** (`npm run shots`) — 15개 상태를 찍어 눈으로 보고 결함 8건 수정.
  표적/위협 라벨 겹침, 위협 색 규칙(적색은 치명 위협 전용), 모드 버튼 대비,
  스폰 즉시 위협 경고, **월드 배치 시드 고정**(새로고침마다 맵이 달라지던 것)
- **아트 패스 1** — "휴가 영상"에서 "전장 영상"으로. 흐린 하늘·먼지빛 팔레트·중립 그레이드
  + 전장의 흔적(포탄공 31 · 불탄 잔해 5 · 지평선 연기 3). 전부 프로시저럴, +3 드로우콜.
  이후 색·대기는 **의도적으로** 프로토타입과 다르다(DEVLOG)
- **아트 패스 2** — 카테고리별 디테일(에셋 없이): 도로 마모대·크랙·중앙선, 경작지 패치워크,
  집 창문·문·기초띠·처마 그늘·마당 울타리, 트럭 부품 12종, 덤불·수관 형태 변종
- **T8a 완료** — 자폭 돌입 판정. 무장이 아니라 기체가 탄이다: 트럭에 박으면 기폭,
  표적은 전소 잔해+연기로 남고(열화상에서도 식는다), 정지 화면에 `TGT DOWN`.
  핵심 루프(격파→확인→SP)의 첫 칸이 채워졌다
- **T8b 완료** — 작전 구역(반경 490m) + 3초 유예. 이탈은 벽이 아니라 **신호**로 막힌다 —
  화면이 무너지다 끊긴다. 지평선 스커트로 맵 끝이 더는 보이지 않는다
- **T8c 완료 — 핵심 루프 완주.** M2 「강철 사냥」 1차수: 접속 → 비행 → 격파/손실 →
  SIGNAL LOST → 디브리핑(실패 원인 1줄 + 권고 자동 조립) → 재출격.
  "실패가 가르친다"(GDD 4.5 규칙 4)가 화면에 있다
- **T9 완료** — SP 경제(트럭 40, 참가상 없음) + 디브리핑 확정 시 저장 + 작전실 화면
  (어시스트 3단 ↔ 비행 모델 매핑, 스틱 모드). 재접속해도 SP·설정이 남는다.
  Save 의 규칙 10 위반(직접 localStorage)도 platform 경유로 수정
- **T10 언어 전환 완료** — 작전실 KO/EN 토글, 즉시 반영 + 저장. 성능 패스 후반
  (중급 폰 60fps)은 **실기 측정 대기** — 이 환경에서 검증 불가
- **🏁 v0.2 (T7~T10) 코드 완료** — 위협 + M2 미션 완주 + 경제 루프.
  남은 판정 두 가지는 사람 몫: 폰 실기 fps, 재미
- **아트 패스 3 (화질)** — 실기 피드백 반영. 내부 해상도 960×540 기본(작전실에서
  아날로그 480p 선택 가능), 상시 노이즈 절반 이하(열화는 신호 상태의 정보),
  안개 150/950m, 하늘 구름층, 지면 텍스처 2배, 채도 복원.
  숨은 버그: 지평선 스커트가 저지대 지형을 덮고 있었다 → 링으로 교체
- **데모 패스** — 첫 미션 인트로부터 한 판: 브리핑(03 문서 규격, 타이핑 연출) →
  스폰이 표적 방향 → HUD `TGT 0/1` → 항법·위협 무전 1줄 → 격파 → 고스트 확인 무전 → 정산
- **플레이스루 밸런스 패스** — 자동조종으로 직접 두 판 플레이 → "조언대로 해도 죽는"
  첫 미션을 발견·수정(A1 조준 1.6초·도로에서 45m). 조언 판 10초 완주 / 직진 판은
  경고 2초 후 격추(학습 루프). `npm run play` 가 이 곡선을 회귀 테스트로 고정
- **게임 셸** — 부팅 흐름이 게임이 됐다: 타이틀(무신호 노이즈 배경 + 글리치 로고) →
  최초 1회 프롤로그(03 문서 1막, 타이핑 연출) → 작전실 → **격납고**(SVG 청사진 기체 카드,
  보급 요청 = SP 차감 + 즉시 배치). 호넷-10 은 배터리 ×1.3 — 시간만 판다(규칙 5)
- **데모의 끝** — Ch.1 주제(확인 킬 학습)로 성공/실패를 닫는다. 완수: 정산 내역
  (격파 40 / 고스트 확인 +40 / 첫 실적 +100)과 바딤의 교육이 같은 화면에 →
  아웃트로(장관의 보급 승인 → 「데모는 여기까지」 → 격납고). 실패: 원인·권고 +
  "기체는 소모품이다. 너는 아니고." 작전실이 완수를 표시하고 출격이 「재도전」이 된다
- **경제를 게임에 붙였다** — 플레이 세션 계측(`npm run session`)에서 "보상이 가리키는 곳이
  비어 있다"가 드러났다(유일한 구매처의 이점이 이 미션에서 안 쓰임). 카메라를 문서대로
  **사다리**로 되돌리고(흑백 기본 → 컬러 300 → 열화상 1,800), 기체 손실 페널티(가격 5%)를
  살렸다. 이제 게임은 흑백으로 시작해 **컬러를 번다**
- **아트 패스 4 (지형 11회 반복)** — 고정 3시점을 찍고 고치기를 11회(개선 8 · 회귀 수정 3).
  초목을 균일 살포 → **토지 피복**(밭은 열고 경계에 산울타리), 지형에 **능선**,
  원경 능선·수목림, 밭을 **회전 필지 격자**로, 텍스처/정점색 역할 분리, 필지별 작물색,
  하늘 2겹 구름 + 태양 글로우, 버섯 수관 수정. 삼각형은 풀에서 빼서 지형에 옮겼다
- Playwright **113종** 통과, 드로우콜 84 / 삼각형 277k
- 다음: **사람 판정 2가지** — 폰 실기 fps(`/` 에서 측정, 45 미만이면 풀 인스턴스 →
  그림자 해상도 → 덤불 수 순으로 조정), 그리고 재미

### 검증 완료된 수치
| 항목 | 실측 |
|---|---|
| 드로우콜 / 삼각형 | **116 / 262k** (예산 <120 통과) |
| 프로 모드 호버 드리프트 | **0.0000m** (10초) |
| 프로 모드 최고속도 | **73.8km/h** |
| 아케이드 전진 속도 | **79.2km/h** (3초) |
| 아케이드 고도 유지 오차 | **0.08m** |
| 인스턴스 총량 | 43,344 |

## 3. 문서 지도

| 파일 | 내용 | 언제 |
|---|---|---|
| `docs/02_DEV_SPEC_web.md` | **개발 기준서** — 스택, 구조, 검증된 상수, 성능 예산, T1~T9 | 항상 (최우선) |
| `docs/07_PROTOTYPE_NOTES.md` | 프로토타입에 뭐가 있고 뭘 그대로 옮겨야 하는지 | 이식 작업 시 (T2~T6) |
| `docs/01_GDD.md` | 게임 전체 설계 — 미션 9종, 위협 16종, 편대 시스템, 경제/보상 | 시스템 구현 시 |
| `docs/04_STYLE_UIUX.md` | 화면 규격, 컬러 토큰 8색, 금지 목록, 와이어프레임 11종 | UI 작업 시 |
| `docs/05_DATA_BALANCE.md` | 데이터 스키마, 저장 JSON, SP 수치, 재고제 | 데이터·경제 작업 시 |
| `docs/06_ART_REFERENCE.md` | 실제 드론 영상 분석 → 셰이더/HUD 실측 규격 | 렌더·HUD 작업 시 |
| `docs/03_NARRATIVE.md` | 3막 9챕터, 캐릭터, 브리핑 규격 | 텍스트·연출 작업 시 |
| `docs/strings_master.csv` | 로컬라이즈 원본 (KO/EN) | i18n 작업 시 |

⚠️ Unity 기반 개발 설계서 v0.1은 폐기됨. 스택은 **Three.js 확정**.

## 4. 절대 규칙

1. **검증된 물리·렌더 상수를 임의로 바꾸지 말 것** (02 문서 4장). 바꿔야 하면 근거 수치와 함께 `DEVLOG.md`에 기록.
2. **인스턴싱 필수** — 반복 오브젝트를 개별 Mesh로 만들면 드로우콜이 1,940까지 폭증(실측). 02 문서 5장.
3. **에셋(3D 모델·텍스처 파일) 도입 금지** — 재미 검증 전까지 프로시저럴 유지.
4. **UI 금지 목록 준수** (04 문서 6장): 라운드 버튼, 그라데이션, 그림자, 코인 아이콘, "축하합니다!" 류. 판정 기준 — *"이게 야전 단말기 화면에 있을 법한가?"*
5. **수익화 원칙**: 유료는 시간만 판다. 성능 P2W·확률형 상자·FOMO 타이머 금지.
6. **하드코딩 금지**: 문자열은 i18n, 튜닝 값은 `src/data/`.
7. **매 태스크 종료 시**: `npm run verify` → 스크린샷 확인 → 커밋 → `DEVLOG.md` 갱신.

## 5. 개발 시작

```bash
npm install
npm run dev        # http://localhost:5173 (host 노출 — 폰에서 같은 망으로 접속 가능)
                   #   /                → 코드베이스
                   #   /prototype.html  → 프로토타입 기준선
```

`/prototype.html` 은 `prototype/signal_lost_fpv.html` 을 **가공 없이 복사**한 것이다
(`tools/sync-demo.js` 가 dev/build 전에 `public/prototype.html` 로 넣는다).
원본은 수정 금지이고, 배포본이 원본과 바이트 단위로 같은지는 테스트가 지킨다.

| 명령 | 하는 일 |
|---|---|
| `npm run dev` | Vite 개발 서버 |
| `npm run build` | 타입 체크 + 프로덕션 빌드 (`dist/`) |
| `npm run typecheck` | `tsc --noEmit` |
| `npm run harness` | 헤드리스 씬 생성 검증 (`tools/harness.js`) |
| `npm run perf` | **프로토타입**의 드로우콜/삼각형 실측 |
| `npm run scene` | **코드베이스**의 드로우콜/삼각형 실측 — 예산(<120) 초과 시 종료 코드 1 |
| `npm run compare` | 프로토타입 ↔ 코드베이스를 같은 조건으로 렌더해 스크린샷 2장 저장 |
| `npm test` | Playwright (빌드 → preview → 검증 → 스크린샷) |
| `npm run verify` | 위 4종 일괄 — **커밋 전 필수** |

스크린샷은 `tests/__screenshots__/` 에 남는다.

## 5-1. 지금 할 일

```
사람 판정 (코드로 못 하는 것):
  1. 폰 실기 fps — https://drone-azure-rho.vercel.app/ 에서 측정 (/prototype.html 아님)
     45 미만이면: 풀 인스턴스 수 → 그림자 해상도 → 덤불 수 순으로 조정
  2. 데모 완주 피드백 — 시작(타이틀→프롤로그→브리핑)부터 끝(디브리핑→아웃트로)까지·화질·재미

다음 코드 작업 (v0.3, 재미 검증 후):
  BDA 확인을 플레이어 책임으로(정찰기 촬영·귀환 — 지금은 Ch.1 학습용 자동 확인)
  · Ch.2 미션 추가(미션 레지스트리 — 지금은 M2_1 하드코딩)
  · **회수 귀환 미션 타입** — 없으면 T2+ 기체가 손해다(손실 페널티만 붙고 이점이 안 쓰임)
  · 재고제(T2 6대·20분 입고 — 미션이 길어진 뒤에) · 신뢰도 티어
  · 별점/도전 배수 · 위협 +3종(D1 디코이) · 사운드 3종 · 재출격 GPU 리소스 정리
```

## 6. 워크플로우

### 6.1 브랜치

| 브랜치 | 역할 | Vercel |
|---|---|---|
| `main` | 안정판. 재미 검증·실기 확인이 끝난 것만 올린다 | **프로덕션** 배포 |
| `develop` | **평소 개발은 여기서 한다.** T2, T3… 태스크 커밋이 쌓인다 | **프리뷰** 배포 (커밋마다 URL) |
| `claude/*` | 에이전트 세션 작업 브랜치 | 배포 안 함 (`vercel.json` 에서 차단) |

마일스톤(v0.1 = T1~T6 등)이 끝나면 `develop` → `main` 병합.

```bash
git checkout develop
# ... 작업 ...
npm run verify          # 커밋 전 필수
git commit -m "T2: prototype module port"
git push origin develop # → Vercel 프리뷰 URL 자동 생성 → 폰에서 확인
```

### 6.2 배포 (연결 완료)

| | |
|---|---|
| 프로덕션 | https://drone-azure-rho.vercel.app (`main`) — `/` 는 코드베이스, `/prototype.html` 은 기준선 |
| 프리뷰 | `develop` push마다 URL 자동 생성 |
| 기본 브랜치 | `main` |

설정은 전부 `vercel.json` 에 있다 (Framework `vite` / Build `npm run build` / Output `dist`,
Node 22는 `package.json` 의 `engines`). **환경변수는 하나도 쓰지 않는다.**

### 6.3 배포 확인

HUD 우하단에 `브랜치 커밋해시` 가 찍힌다 (예: `develop a1b2c3d`).
폰에서 열었을 때 이 값이 방금 push한 커밋과 같아야 반영된 화면이다.
콘솔에서는 `__debug.build` 로도 확인할 수 있다.

### 6.4 CI

`.github/workflows/ci.yml` 이 `main`/`develop` push와 PR에서 `typecheck → build → harness → perf → playwright` 를 돌린다.
**드로우콜 예산(<120) 초과와 Playwright 실패가 여기서 막힌다** — Vercel은 빌드만 보기 때문에 이 게이트가 없으면 깨진 화면이 그대로 배포된다.
스크린샷은 Actions 아티팩트로 올라온다.

### 6.5 기타

- 모바일 Claude Code ↔ 맥북(tmux + Tailscale) 원격
- 커밋 규칙: `T3: flight models`
- 세션 간 컨텍스트 유실 방지: `DEVLOG.md` 누적

## 6-1. 데이터베이스 — 미도입

**서버도 DB 도 없다.** 저장은 localStorage 뿐이고(`src/core/Save.ts`), 05 문서 1장이 규정한
대로 v1.0까지 이 상태를 유지한다.

계정 없는 클라우드 세이브(기기 간 이어하기)를 한 번 구현했다가 **걷어냈다** (2026-08-26, 사용자 결정).
필요해질 때 되살리면 되고, 되살릴 때 읽어야 할 것:

- DEVLOG 의 `5f253a8` 항목 — 설계(기기별 시크릿 / 이어하기 코드 / `rev` 낙관적 잠금)와
  **Vercel 배포에서만 재현되는 함정 3종**이 정리돼 있다
- 저장이 `Save.ts` 한 파일에 격리돼 있고 `schemaVersion` 이 이미 붙어 있으므로,
  지금 구조를 미리 바꿔 둘 필요는 없다

## 7. 우선 확인이 필요한 것

- **폰 실기 fps** — **`/` 우상단**을 볼 것. 45 미만이면 풀 인스턴스 수 → 그림자 해상도 → 덤불 수 순으로 조정.
  ⚠️ **`/prototype.html` 의 fps 표시는 쓰면 안 된다.** 클램프된 dt 를 누적해 계산하는 버그가 있어
  **20fps 아래로 내려가지 않는다**(실측: 진짜 1.1fps 인데 22fps 로 표시). 07 문서 4장 참조
- **화면 감성 파라미터 확정** — 프로토타입 튜닝 패널에서 프리셋 A/B/C 중 선택 후 미세조정 → JSON 복사 → `src/data/postfx.ts`에 고정
