{
  "name": "signal-lost-fpv",
  "private": true,
  "version": "0.1.0",
  "description": "저화질 FPV 드론 전쟁 시뮬레이터 — 모바일 웹 우선",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "predev": "node tools/sync-demo.js",
    "dev": "vite",
    "prebuild": "node tools/sync-demo.js",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "harness": "node tools/harness.js",
    "perf": "node tools/perf.js",
    "scene": "node tools/scene-check.mjs",
    "compare": "node tools/compare-demo.mjs",
    "test": "playwright test",
    "verify": "npm run typecheck && npm run build && npm run harness && npm run perf && npm run scene && npm run test"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.3.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
