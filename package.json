{
  "name": "signal-lost-fpv",
  "private": true,
  "version": "0.1.0",
  "description": "저화질 FPV 드론 전쟁 시뮬레이터 — 모바일 웹 우선",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "harness": "node tools/harness.js",
    "perf": "node tools/perf.js",
    "test": "playwright test",
    "verify": "npm run typecheck && npm run build && npm run perf && npm run test"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.3.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
